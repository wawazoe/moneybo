<%= render "shared/header" %>

<div class='top_page'>
<h1>トップページ</h1>
<%= link_to "#{current_user.nickname}さん" %>の家計簿
<ul>
  <li><%= link_to "支出を入力する", new_gife_path(current_user.id) %></li>
  <li><%= link_to "支出を編集する", edit_gife_path(current_user.id) %></li>
   <%#= link_to "チャットを終了する", room_path(@room), method: :delete %>
  <li><%= link_to "収入を入力する", new_get_path(current_user.id) %></li>
  <li><%= link_to "収入を編集する", edit_get_path(current_user.id) %></li>

</ul>
</div>

<%= render "shared/footer" %>